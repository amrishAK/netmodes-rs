/// UDP transport module placeholder.
///
/// This module is intentionally minimal until UDP client/server handlers are implemented.
pub mod placeholder {
    /// Marker value used to keep the UDP module linked in the crate.
    pub const ENABLED: bool = true;
}
